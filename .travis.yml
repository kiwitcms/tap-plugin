sudo: required
language: python
cache: pip
python:
  - 3.6
env:
  - MAKE=ci
  - MAKE=check-build
install:
  - pip install -r devel.txt
script:
  - make $MAKE

after_success:
  - |
        pip install coveralls
        coveralls

notifications:
  email:
    on_failure: change
    on_success: never
